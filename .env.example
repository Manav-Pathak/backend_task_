POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# For FastAPI/SQLAlchemy connecting from inside Docker.
# Keep this database name the same as POSTGRES_DB.
DATABASE_URL=

# Optional: use only when running FastAPI outside Docker against the host port.
LOCAL_DATABASE_URL=

PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=

JWT_ACCESS_TOKEN_MINUTES=60
FRONTEND_ORIGINS=http://localhost:8000
JWT_SECRET_KEY=dev-secret-key-change-this-before-production
